<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.username %></td>
        <td><%= post.content %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>

<%= link_to 'New Post', new_post_path %>


<%= form_with model: @post do |form|  %>
    <%= form.label :username %>
    <%= form.text_field :username %>
    <%= form.label :content %>
    <%= form.text_field :content %>
    <%= form.submit 'Create', data: { confirm: "Do you want to create the post?" } %>
<% end %>
